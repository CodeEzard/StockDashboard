services:
  - type: web
    name: stock-dashboard-backend
    env: docker
    plan: free
    dockerContext: .
    dockerfilePath: Dockerfile
    healthCheckPath: /companies
    envVars:
      - key: PORT
        value: 8000
