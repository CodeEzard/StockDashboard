services:
  - type: web
    name: stock-dashboard-backend
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    healthCheckPath: /companies
    envVars:
      - key: PORT
        value: 8000
